# Nginx Configuration Template for BUOGS Portfolio
server {
    listen 80;
    server_name buogs.com www.buogs.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name buogs.com www.buogs.com;

    ssl_certificate /etc/letsencrypt/live/buogs.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/buogs.com/privkey.pem;

    client_max_body_size 60M;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Enable HTTP range requests for media streaming
    location /media/ {
        proxy_pass http://127.0.0.1:3000/media/;
        proxy_force_ranges on;
    }
}
